/* SUPER LOVABLE V2 — adaptador do backend GitHub */
const SETTINGS_KEY='slv2_github_api_settings';
const DEFAULT_SETTINGS=Object.freeze({baseUrl:'https://painel-super-lov.lovable.app',connectPath:'/api/github/connect',statusPath:'/api/github/status',repositoriesPath:'/api/github/repositories',branchesPath:'/api/github/branches',disconnectPath:'/api/github/disconnect',timeoutMs:15000,simulatorEnabled:false});
function storageGet(){return new Promise((resolve,reject)=>chrome.storage.local.get(SETTINGS_KEY,r=>{const e=chrome.runtime.lastError;e?reject(new Error(e.message)):resolve(r?.[SETTINGS_KEY]||null);}));}
function storageSet(value){return new Promise((resolve,reject)=>chrome.storage.local.set({[SETTINGS_KEY]:value},()=>{const e=chrome.runtime.lastError;e?reject(new Error(e.message)):resolve();}));}
export async function getGithubApiSettings(){return{...DEFAULT_SETTINGS,...((await storageGet())||{})};}
export async function saveGithubApiSettings(input={}){const current=await getGithubApiSettings(),baseUrl=String(input.baseUrl||current.baseUrl).trim().replace(/\/+$/,'');if(!/^https:\/\//i.test(baseUrl))throw new Error('A API do GitHub deve usar HTTPS.');const next={...current,...input,baseUrl,timeoutMs:Math.max(3000,Math.min(Number(input.timeoutMs)||current.timeoutMs,30000))};await storageSet(next);return next;}
function simulatedRepositories(){return[{id:1,fullName:'usuario/projeto-lovable',name:'projeto-lovable',owner:'usuario',private:true,defaultBranch:'main',permissions:{read:true,write:true}},{id:2,fullName:'usuario/landing-page',name:'landing-page',owner:'usuario',private:false,defaultBranch:'main',permissions:{read:true,write:true}}];}
async function request(path,options={}){const settings=await getGithubApiSettings();if(settings.simulatorEnabled){if(path===settings.statusPath)return{status:'connected',accountLogin:'usuario',accountName:'Conta de teste',installationId:'simulated',simulated:true};if(path===settings.repositoriesPath)return{repositories:simulatedRepositories(),simulated:true};if(path===settings.branchesPath)return{branches:['main','develop','staging'],simulated:true};if(path===settings.connectPath)return{authorizationUrl:null,simulated:true};if(path===settings.disconnectPath)return{success:true,simulated:true};}
 const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),settings.timeoutMs);try{const response=await fetch(`${settings.baseUrl}${path}`,{method:options.method||'GET',headers:{'Content-Type':'application/json','X-Super-Lovable-Client':'chrome-extension-v2'},body:options.body?JSON.stringify(options.body):undefined,credentials:'omit',cache:'no-store',signal:controller.signal});const data=await response.json().catch(()=>({}));if(!response.ok)throw new Error(data.message||`Servidor retornou ${response.status}.`);if(path===settings.connectPath&&!data.authorizationUrl)throw new Error('A conexão real com o GitHub ainda não está configurada no servidor.');return data;}catch(error){if(error?.name==='AbortError')throw new Error('Tempo esgotado ao conectar com o GitHub.');if(error instanceof TypeError)throw new Error('Não foi possível acessar o servidor de conexão do GitHub.');throw error;}finally{clearTimeout(timer);}}
export async function beginGithubConnection(){const s=await getGithubApiSettings();return request(s.connectPath,{method:'POST'});}export async function fetchGithubStatus(){const s=await getGithubApiSettings();return request(s.statusPath);}export async function fetchGithubRepositories(){const s=await getGithubApiSettings();return request(s.repositoriesPath);}export async function fetchGithubBranches(repositoryFullName){const s=await getGithubApiSettings();return request(s.branchesPath,{method:'POST',body:{repository:repositoryFullName}});}export async function disconnectGithubRemote(){const s=await getGithubApiSettings();return request(s.disconnectPath,{method:'POST'});}
